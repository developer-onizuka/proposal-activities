# ゴール

一般のITシステム提案時と同様に、生成AI基盤のシステム設計においても、ビジネスの要件定義から始め、基本設計、詳細設計を行うことになる。当然ながら、多くの場合はこれでは終わらず、物理的な制約（GPUメモリ、帯域、コスト）を計算機科学をはじめとするハードウェア、ソフトウェアの知見によって最適化が必要とされ、最終的にクラウドネイティブな技術によって効果的な運用の改善が求められ、顧客ビジネスに多大な影響を及ぼす存在となる。本ドキュメントは、これら一連の流れを学び、どのように生成AI基盤を構築していくのかの一端を学ぶものである。

<br>

# 目次
| 章 | 章のタイトル・目的 | 入力 (Input) | 出力 (Output) |
| :--- | :--- | :--- | :--- |
| **第1章** | **【要件定義】**<br>[ビジネス要件をインフラ仕様へ変換するワークロード分析](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC1%E7%AB%A0%E8%A6%81%E4%BB%B6%E5%AE%9A%E7%BE%A9%E3%83%93%E3%82%B8%E3%83%8D%E3%82%B9%E8%A6%81%E4%BB%B6%E3%82%92%E3%82%A4%E3%83%B3%E3%83%95%E3%83%A9%E4%BB%95%E6%A7%98%E3%81%B8%E5%A4%89%E6%8F%9B%E3%81%99%E3%82%8B%E3%83%AF%E3%83%BC%E3%82%AF%E3%83%AD%E3%83%BC%E3%83%89%E5%88%86%E6%9E%90) | ・ビジネス課題（要約、推論、RAG等）<br>・タスク難易度による知能レベル要求<br>・予算制約とTCOの許容範囲 | **・モデルパラメータ数（8B/70B等）**<br>**・実効GPUメモリ容量の算出結果**<br>**・推奨GPU種別と必要枚数** |
| **第2章** | **【基本設計】**<br>[投資対効果（ROI）を最大化するスループットとコストの最適化](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC2%E7%AB%A0%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%8A%95%E8%B3%87%E5%AF%BE%E5%8A%B9%E6%9E%9Croi%E3%82%92%E6%9C%80%E5%A4%A7%E5%8C%96%E3%81%99%E3%82%8B%E3%82%B9%E3%83%AB%E3%83%BC%E3%83%97%E3%83%83%E3%83%88%E3%81%A8%E3%82%B3%E3%82%B9%E3%83%88%E3%81%AE%E6%9C%80%E9%81%A9%E5%8C%96) | ・第1章で選定したモデルとGPU<br>・サービス品質保証（SLA/応答速度）<br>・想定される同時リクエスト数 | **・最適バッチサイズ**<br>**・理論的な性能飽和点**<br>**・トークンあたりの最小単価設計** |
| **第3章** | **【詳細設計】**<br>[推論基盤のアーキテクチャ選定](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC3%E7%AB%A0%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%8E%A8%E8%AB%96%E5%9F%BA%E7%9B%A4%E3%81%AE%E3%82%A2%E3%83%BC%E3%82%AD%E3%83%86%E3%82%AF%E3%83%81%E3%83%A3%E9%81%B8%E5%AE%9A) | ・目標スループットとSLA<br>・モデル刷新の頻度（柔軟性の要求）<br>・商用環境の複雑性（アンサンブル等） | **・推論配信基盤（Triton / vLLM API）**<br>**・バックエンドエンジン（vLLM / TensorRT-LLM）**<br>**・API通信プロトコル（gRPC/HTTP）** |
| **第4章** | **【実装・最適化】**<br>[動的メモリ管理とI/Oパイプラインの非同期設計](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC4%E7%AB%A0%E5%AE%9F%E8%A3%85%E6%9C%80%E9%81%A9%E5%8C%96%E5%8B%95%E7%9A%84%E3%83%A1%E3%83%A2%E3%83%AA%E7%AE%A1%E7%90%86%E3%81%A8io%E3%83%91%E3%82%A4%E3%83%97%E3%83%A9%E3%82%A4%E3%83%B3%E3%81%AE%E9%9D%9E%E5%90%8C%E6%9C%9F%E8%A8%AD%E8%A8%88) | ・選択した推論基盤とエンジン<br>・実行時のOOM（メモリ不足）発生状況<br>・CPU/データ転送のボトルネック | **・PagedAttentionのブロック管理設定**<br>**・非同期I/O（プリフェッチ）パイプライン**<br>**・TFRecord等のバイナリデータ形式採用** |
| **第5章** | **【コスト最適化】**<br>[モデル量子化の検討](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC5%E7%AB%A0%E3%82%B3%E3%82%B9%E3%83%88%E6%9C%80%E9%81%A9%E5%8C%96%E3%83%A2%E3%83%87%E3%83%AB%E9%87%8F%E5%AD%90%E5%8C%96%E3%81%AE%E6%A4%9C%E8%A8%8E) | ・ハードウェアリソースの物理的限界<br>・精度維持とスループット向上のトレードオフ<br>・長文コンテキスト（KVキャッシュ）の負荷 | **・量子化手法（AWQ / GPTQ / FP8）**<br>**・KVキャッシュ量子化（INT8/FP8）設定**<br>**・GPU集約によるインフラコスト削減案** |
| **第6章** | **【拡張設計】**<br>[マルチノード・クラスタと高速インターコネクト](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC6%E7%AB%A0%E6%8B%A1%E5%BC%B5%E8%A8%AD%E8%A8%88%E3%83%9E%E3%83%AB%E3%83%81%E3%83%8E%E3%83%BC%E3%83%89%E3%82%AF%E3%83%A9%E3%82%B9%E3%82%BF%E3%81%A8%E9%AB%98%E9%80%9F%E3%82%A4%E3%83%B3%E3%82%BF%E3%83%BC%E3%82%B3%E3%83%8D%E3%82%AF%E3%83%88) | ・単一ノードの計算限界（スケールアウト要求）<br>・ノード間通信によるStarvation（停滞）<br>・大規模学習・推論のワークロード特性 | **・高速インターコネクト（InfiniBand / RoCE）**<br>**・RDMA / NCCL 最適化パラメーター**<br>**・ノンブロッキング・ネットワークトポロジ** |
| **第7章** | **【高度な運用】**<br>[Cloud Nativeな実装](https://github.com/developer-onizuka/proposal-activities?tab=readme-ov-file#%E7%AC%AC7%E7%AB%A0%E3%83%87%E3%83%97%E3%83%AD%E3%82%A4cloud-native%E3%81%AA%E6%A9%9F%E5%8B%95%E5%8A%9B%E3%81%AE%E5%AE%9F%E8%A3%85) | ・トラフィックの需要変動（スパイク）<br>・GPUの起動遅延（コールドスタート）問題<br>・スポットインスタンス等のコスト削減機会 | **・KEDAによるカスタムメトリクス（KV Cache等）**<br>**・Image Streamingによる起動高速化設計**<br>**・PriorityClassを用いたOver-provisioning構成** |

<br>

本設計プロセスは、抽象的なビジネス要件を物理的なインフラ実装へと落とし込む一連のパイプラインとして定義される。各章は独立した技術要素ではなく、前段の出力を受けて後段の最適化を行うという強固な依存関係の上に成立している。

### 1. 戦略フェーズ：ビジネス要件の物理定義（第1章〜第2章）
設計の起点は技術ではなく、ビジネス課題である。第1章で定義された知能レベル（モデルサイズ）と物理制約（GPUメモリ容量）は、インフラの最小単位を決定する。続く第2章では、この物理リソース上で許容される応答速度（SLA）を定義することで、最もコスト効率の高い動作点（バッチサイズ）を算出する。ここでの出力は、以降の全工程における必達すべき性能目標となる。



### 2. 基盤フェーズ：ソフトウェアによる効率の極限追求（第3章〜第5章）
第3章では、前段で定めた性能目標を達成するために最適な推論基盤（Triton/vLLM等）を選択する。第4章は、選択した基盤のポテンシャルを阻害する要因（メモリ断片化やI/Oボトルネック）を、PagedAttentionや非同期パイプラインによって排除する工程である。さらに第5章において、モデルそのものを量子化によって軽量化し、1枚のGPUに詰め込める知能密度を最大化する。この一連のプロセスにより、1トークンあたりの生成コストを物理的な限界まで引き下げる。



### 3. 拡張・運用フェーズ：スケールと持続性の担保（第6章〜第7章）
単一の計算資源で処理不能な規模（超大規模モデルや膨大なトラフィック）に対し、第6章ではマルチノード化による空間的拡張を行う。ここではRDMA等の高速インターコネクトを用い、通信による演算の停滞を徹底して排除する。最終段階である第7章では、これらの高度な計算資源をCloud Native（Kubernetes）の管理下に置き、需要変動に合わせた時間的拡張（オートスケーリング）を実現する。

<br>
<br>
<br>

# 第1章：【要件定義】ビジネス要件をインフラ仕様へ変換するワークロード分析

### サマリー
生成AIインフラの設計において最も重要な最初の意思決定は、顧客の抽象的なビジネス要件を、物理的なインフラ仕様へと論理的に翻訳するプロセスである。この設計は、単に最新のGPUを並べることではなく、タスクの複雑性、精度、コストの三原則から逆算される収束プロセスとして定義される。

---

### 1. タスクの難易度によるモデルサイズの絞り込み
まず、解決すべきビジネス課題の複雑性から、必要十分な知能（パラメータ数）を特定する。定型的な要約や単純な抽出タスクであれば8B前後の軽量モデルで十分であるが、高度な論理推論やプログラミング支援が必要な場合は70Bクラス以上の中・重量級モデルが選択肢に入る。ここで重要なのは、知能のオーバースペックを避けることである。パラメータ数が増えるほど、後述するインフラコストとレイテンシが指数関数的に増大するためである。

### 2. 物理制約：実効GPUメモリ容量の算出
モデルが決まれば、物理的なメモリ要件を算出する。FP16精度（2バイト/パラメータ）を基準とした場合、モデルの重みだけで **パラメータ数 × 2バイト** のGPUメモリを占有する。しかし、実務設計で重要なのは、入力トークン長に比例して肥大化するKVキャッシュ分の動的バッファを加味した実効メモリの計算である。特にRAG（外部検索）などで長大なコンテキストを扱う場合、モデル自体よりもこのキャッシュ領域がボトルネックとなり、GPUの枚数やメモリ帯域（HBM）への要求を決定づける。

### 3. パフォーマンス指標とGPUの選定ロジック
最終的なデバイス選定は、目標とするトークン生成速度とTCO（総保有コスト）のバランスで決まる。

* **推論のスループット重視**: コスト効率を優先し、中規模以下のモデルを量子化して運用する場合、L4やL40SといったシングルスロットGPUを並列化する構成が有効である。
* **学習および超低レイテンシ推論**: 大規模LLMの学習や、極めて高い応答速度が求められる商用環境では、高帯域メモリを備え、GPU間を高速なNVLinkで結合できるA100やH100/H200が必然の選択となる。



このように、ビジネス課題から必要な知能（モデル）、そして物理実効メモリの制約を経て性能限界（GPU種別）へと至るロジックを十分検討することで、単なるスペック提案ではない経営と技術を繋ぐ必然性の高いアーキテクチャが完成する。

---

#### Q1: 特定のAIワークロードに対して、適切なGPUタイプと数をどのように決定するか？

**回答の論理構成例：**
例えば、Llama-3 70Bモデルを用いた社内ドキュメント検索（RAG）システムを構築する場合、以下のステップでモデルとインフラを絞り込む。

1. **PoCによる精度検証**
   まずは8Bと70Bの両モデルで精度比較を行い、業務要件を満たす最低ラインのパラメータ数を特定する。仮に70Bが必要と判断された場合、FP16精度ではモデル単体で140GB、KVキャッシュを含めると160GB以上のGPUメモリが必要となる。
2. **インフラの試算とリソース提示**
   目標応答速度が1秒間に20トークンであれば、メモリ帯域（HBM）が重要になる。80GBのGPUメモリを持つH100を2枚、NVLinkで接続する構成を基本案として提示する。
3. **コスト対精度の代替案提示**
   予算が限られている場合は、モデルの量子化を提案する。70Bを量子化してGPUメモリを圧縮しH100を1枚に抑える案や、あるいは「量子化した8BモデルをL4で回せば、精度は若干落ちるもののコストは1/5に抑えられる」といった比較案を提示し、ビジネス価値とコストのバランスに基づく意思決定を支援する。


<br><br><br>

# 第2章：【基本設計】投資対効果（ROI）を最大化するスループットとコストの最適化

### サマリー
本章の目的は、第1章で選定したデバイス性能を最大限に引き出し、投資対効果（ROI）を確定させることにある。生成AIインフラの運用コストを最適化する鍵は、計算リソースが遊ばないように配慮しつつ、大量のトークンを処理するスループットを向上させる点にある。しかし、スループットの追求はユーザーの体感速度であるレイテンシとの間に強力なトレードオフを生じさせる。そのため、サービス品質保証（SLA）を遵守しつつ、計算リソースの性能限界を理論的に把握することで、限られた予算内でコスト効率を両立させる最適解を設計に組み込むプロセスを解説する。

---

### 1. バッチサイズによる性能変化のメカニズム
GPUの性能を評価する際、単なる演算理論値だけでなく、メモリからデータを読み出す速度と演算器が計算する速度のどちらがボトルネックになっているかを見極める必要がある。
小規模なバッチサイズ、あるいは1リクエストずつの逐次処理では、演算器の処理能力に対してデータの供給が追いつかないメモリ転送速度の限界に直面する。この状態では、どれほど高価なGPUを導入しても、演算リソースの大部分が待機状態となってリソースが遊んでしまい、コスト効率が著しく低下する。

### 2. スループットとレイテンシの関係
バッチサイズ、つまり一度に並列処理するリクエスト数を拡大すると、1回のデータ読み出しに対して計算密度が高まり、GPUの演算効率は飛躍的に向上する。これがスループットの最大化をもたらす。
しかし、バッチサイズを大きくしすぎると、計算自体の完了を待つ時間が増え、最初の1文字目が出るまでの時間（TTFT）や、全体の応答時間が悪化する。ここで重要となるのが、ビジネス側が設定したSLA（応答速度の許容範囲）である。インフラ設計者は、このSLAの境界線を超えない範囲で、計算リソースの理論的な性能飽和点を狙ってバッチサイズを調整しなければならない。

### 3. コスト効率を両立させる最適動作点の特定
最終的な設計指針は、ハードウェアが持つ計算性能の限界を可視化して把握することである。演算性能が飽和する直前、かつSLAを維持できるポイントこそが、最もトークンあたりの単価が安くなる動作点となる。
このように、ハードウェアの物理的な特性とユーザー体験のバランスを意識し、理論的な裏付けを持って最も効率的に稼働する設定を導き出すことが重要である。



---
性能設計の現場で問われる判断のプロセスを例示する。

#### Q2: 計算リソースが遊ばないように効率を最大化しつつ、応答速度を維持するための具体的なアプローチは何か？

**回答の論理構成例：**
例えば、大規模な顧客基盤を持つ企業のチャットサービスを運用する場合、単一のGPU性能だけでなく、リソース全体の断片化を防ぐ視点が不可欠となる。

1. **性能特性の把握とSLAの定義**
   使用するGPUにおいて、メモリ帯域がボトルネックとなっている領域を特定する。同時に、ビジネス要件として応答速度3秒以内というSLAを定義し、それを維持できる限界のバッチサイズを測定する。
2. **ソフトウェアによるリソースの断片化解消**
   個別のリクエストを単純に各GPUへ割り振ると、あるGPUは高負荷でレイテンシが悪化し、別のGPUはリソースが余るといった断片化が生じる。これを防ぐため、リクエストを集中管理し、各リソースの稼働状況に合わせて配分するオーケストレーションを導入する。
   また、**PagedAttention**を導入し、OSの仮想メモリのようにGPUメモリをブロック単位で動的に管理することで、KVキャッシュによるメモリの内部断片化を排除する。これにより、メモリ利用率を極限まで高め、1枚のGPUで処理可能な同時リクエスト数を最大化させる。
   
3. **継続的バッチング（Continuous Batching）による効率化**
   従来の静的バッチングでは、最も長い回答の完了を待つ間に他の演算リソースが遊んでしまう課題があった。これに対し、推論エンジン側でリクエストの完了を待たずに新たなリクエストを順次詰め込む**継続的バッチング**を採用する。
   
   これにより、GPU内の演算器が遊ぶ時間をゼロに近づけ、SLAを遵守しながらトークンあたりの処理コストを最小化、投資対効果を最大化させる。

<br><br><br>

   
# 第3章：【詳細設計】推論基盤のアーキテクチャ選定

### サマリー
本章の目的は、第2章で算出した理論上の目標値を実現するため、推論基盤を設計することにある。ハードウェアの性能を最大限に引き出すには、Triton Inference Serverのような高度な配信基盤を軸に、プロジェクトの性質に応じてvLLMやTensorRT-LLMをバックエンドとして選択する戦略が一般的である。本章では、これらの組み合わせによる必然的なアーキテクチャの詳細設計プロセスを提示する。

---

### 1. 推論配信基盤の選択：Triton Inference ServerとvLLM（APIサーバーモード）
詳細設計において、推論環境は単なるモデルの実行環境ではなく、ネットワーク越しにリクエストを制御する配信基盤として定義する。

FlaskやFastAPIを用いて自作のWebAPIサーバーを構築することも可能だが、汎用フレームワークではLLM特有のキューイング管理やGPUリソースの最大活用を自前で実装しなければならず、ハードウェアが遊んでしまうリスクが高い。これを回避するため、以下のいずれかの基盤を採用する。

* **Triton Inference Server**
  NVIDIAが提供する多機能な配信基盤である。複数のバックエンドを統合管理でき、gRPC通信やモデルアンサンブルなど、大規模かつ複雑な商用運用に耐えうる柔軟性を持つ。（https://www.youtube.com/watch?v=Dty23tvTUDo）
* **vLLM（APIサーバーモード）**
  vLLMに内蔵されたサーバー機能を直接利用する形態である。Triton Inference Serverほど多機能ではないが、OpenAI互換のAPIを即座に提供できるため、LLM単体での推論配信においては最も軽量かつ効率的な選択肢となる。


### 2. バックエンドエンジンの論理的選択
Triton Inference Serverの背後で動く推論エンジンの選択が、限られたGPUリソースをどれだけ無駄なく使い切り、同時に処理できるリクエスト数をどこまで増やせるかを決定づける。

### Triton Inference Server + vLLM バックエンド：開発効率と柔軟性の両立
vLLMをバックエンドとして組み込む構成である。
* **PagedAttention機能**: OSの仮想メモリのようにKVキャッシュを断片化させずに管理する。これにより、メモリの空き容量を最大限に活用してバッチサイズを拡大し、一度に処理できるユーザー数を物理的に増大させる。
* **モデル対応の即時性**: Pythonベースの柔軟性を活かし、最新のオープンソースモデルを改修なしで即座にAPI化できる。最新技術の検証や、頻繁なモデル刷新が求められる環境において、インフラの機動力を担保する。

### Triton Inference Server + TensorRT-LLM バックエンド：演算性能の極限追求
NVIDIAのハードウェアに特化したTensorRT-LLMをバックエンドに採用する構成である。
* **カーネル最適化と量子化機能**: NVIDIA GPUの演算器（Tensor Core）に最適化された計算処理を行う。FP8等の最新の量子化技術を極限まで活用することで、計算時間を短縮し、圧倒的な低レイテンシを実現する。
* **垂直統合型ビルド**: 事前にモデルを特定のGPUに最適化したエンジンファイルへとコンパイルする工程が必要だが、その分、ハードウェアが持つ理論上の限界性能を限界まで引き出す。大規模・固定モデルの運用において、1トークンあたりの計算コストを最小化する。

---


#### Q3: 推論環境の設計において、ハードウェア性能を最大化するためのバックエンド採択基準は何か？

**回答の論理構成例：**
スループットという単一の指標に固執せず、運用コストや柔軟性を含めたトータルバランスを基準に評価する。

1. **基盤採用の判断**
   自作APIの限界を認識し、Triton Inference ServerやvLLM（APIサーバーモード）などの専用基盤を採用することで、GPUリソースの最大活用を担保する。
2. **検証フェーズ（vLLM）**
   モデル選定が流動的な段階では、開発の機動力を重視する。vLLMの柔軟性を活かし、SLAを維持できる構成を迅速に立ち上げる。
3. **本番運用フェーズ（TensorRT-LLM）**
   スループットがROIの決定打となる段階では、TensorRT-LLMによる最適化を設計に組み込む。ビルドの手間という運用コストを支払ってでも、リソースを限界まで詰め込み、GPU枚数を削減する判断を行う。

<br><br><br>

# 第4章：【実装・最適化】動的メモリ管理とI/Oパイプラインの非同期設計

## サマリー
本章の目的は、計算リソースが確保されているにもかかわらず、システムが停止、あるいは低速化する実装上のボトルネックを排除することにある。第3章で選定した推論エンジンが持つポテンシャルを物理的な限界まで引き出すため、KVキャッシュの占有予測に基づく動的制御と、前処理・データ転送の遅延を演算の裏側で隠蔽するI/Oパイプラインの非同期設計手法を詳解する。

---

### 1. 実行時GPUメモリ管理の最適
理論上は収まるはずのGPUメモリが、実行時にOut of Memory（OOM）を起こす主因は、推論中に動的に増大するKVキャッシュの予約領域にある。

* **PagedAttention によるブロック管理**
  メモリを OS の仮想メモリ（ページ管理）のように、一定のトークン数を 1 ブロック（例：16 トークン）とした単位で管理します。各リクエストに対して事前に巨大な領域を予約せず、実際の生成トークン数に応じてリアルタイムで物理メモリ（ブロック）を割り当てることで、内部断片化を最小限に抑え、物理的なGPUメモリ容量を超える同時実行数を実現します。

> [!NOTE]
> この「ブロック」は、OS のメモリ管理における 4KB といった物理的な容量ではなく、**論理的なトークンの数**で定義されます。これにより、モデルのパラメータ数や構造が異なっても、エンジニアは共通の尺度でメモリ消費を管理・予測できます。
* **KVキャッシュ・スワッピングの導入**
  GPUメモリが枯渇しそうになった際、進行中のリクエストを破棄するのではなく、一部のキャッシュ（メモリブロック）を一時的にホストメモリ（CPU側）へ退避させる。これにより、突発的な長文回答リクエストが混入しても、システム全体を停止させない弾力的な実装を行う。



### 2. I/Oパイプラインの非同期設計：GPU Starvationの解消
GPUの演算速度に対し、CPU側での処理や物理的なデータ転送がボトルネックとなる状態を、推論サーバー主導のプリフェッチとデータの集約によって解消する。

* **推論サーバーによる非同期制御とプリフェッチ**
  演算プロセスとは別に、ホスト側でデータ準備専用のスレッドを稼働させ、GPUの演算器を常にデータで満たし続けるラインを構築する。
    * **非同期コピーの活用**: PyTorch の `non_blocking=True` 指定や推論エンジンの内部スケジューラを通じ、データ転送の完了を待たずに次の CPU 処理（デコードやリクエストの集約）へ移行する設計を確立する。
* **データ形式の最適化とシリアライズの非同期化**
  JSON 等のテキスト形式ではなく、バイナリ形式（TFRecord や Protocol Buffers）を採用することで、CPU によるデータ解析（デシリアライズ）のオーバーヘッドを最小化する。
    * **オーバーラップ設計**: GPU が演算を実行している裏側で、ホスト側（CPU）が次順のデータをバイナリ化して準備しておくパイプラインを構築し、演算器へのデータ供給を途切れさせない。

> [!NOTE]
> TFRecordは、大量のデータを一つのバイナリファイルにまとめる、高速読み込み専用のデータ形式である。数百万の小さなファイルを個別に出し入れする際のOS負荷（I/Oボトルネック）を排除し、ストレージ帯域を最大限に活用可能。データはシリアライズされた状態で保持されるため、CPUによる変換負荷が低く、GPU演算の裏側で次順のデータを準備する非同期パイプラインの構築に極めて有効な技術である。

* **推論エンジンの最適化設定**
  利用するフレームワークの特性に合わせ、GPU の演算密度を最大化するパラメーター調整を行う。
    * **並列実行数の制御（Triton: `instance_group`）**: 1 つの GPU 内で複数の推論インスタンスを並列稼働させ、演算コアの空き時間を最小化する。
    * **バッチ密度の最大化（vLLM: `max_num_batched_tokens`）**: 1 回の演算サイクルに投入する合計トークン数を調整し、スループット（効率）とレイテンシ（応答性）の最適なバランスを確保する。
---

#### Q4：モデルが理論上はGPUメモリに収まる計算であるにもかかわらず、実行時に CUDA Out of Memory (OOM) が発生したり、期待したスループットが出ない場合、どの設計不備を疑うか？ 同時に、高トラフィックな推論アーキテクチャにおいて、GPU Starvationを回避するために前後処理レイヤーをどのように設計すべきか？

**回答の論理構成例：**
要件定義に基づく静的なリソース見積もりの正当性を確認するだけでなく、実行時のメモリ予約の粒度とデータ供給の連続性という動的な挙動を基準に再点検を行う。

1. **メモリ使用状況の検証**
   PagedAttention を有効にしている場合でも、1ブロックあたりのトークン数設定が不適切であれば、ブロック内に大きな未使用領域が残る内部断片化が生じる。実行時の平均トークン消費傾向を分析し、物理的な空きメモリを使い切るために設定を最適化する。
2. **推論エンジンの並列・密度設定の妥当性**
   GPUメモリ容量に余裕があるにもかかわらずスループットが低い場合、`instance_group`（Triton）の設定不足により GPU の演算コアを遊ばせているか、あるいは `max_num_batched_tokens`（vLLM）が小さすぎて演算密度が上がっていない可能性がある。スループットとレイテンシのトレードオフを考慮しつつ、ハードウェアを使い切る設定値へ調整する。
3. **データ供給ラインの停滞（GPU Starvation）の特定**
   NVIDIA Nsight Systems 等のプロファイラーを用い、時間軸上で演算と転送がオーバーラップしているかを検証する。GPU の演算列に空白（アイドル時間）が生じ、その直前で CPU 側の前後処理やメモリ転送の完了を待っている状態であれば、非同期パイプラインの設計不備と判断し、プリフェッチやバッチ集約の最適化を行う。上位ライブラリの非同期転送フラグ（`non_blocking=True` 等）を適切に使用し、現在のバッチを計算している間に次順のバッチを GPUメモリへ送り込む計算と通信のオーバーラップが実現されているかを点検する。
4. **前後処理の非同期デカップリング**
トークナイズ等の CPU 負荷の高い処理が GPU ストリームを直接ブロックしないよう、前後処理を演算プロセスから論理的に分離する。また、JSON 等の解析負荷を避けるため TFRecord 等のバイナリ形式を採用し、演算の裏側で次順のデータ準備が完結する非同期構造（オーバーラップ）を設計する。


<br><br><br>


# 第5章：【コスト最適化】モデル量子化の検討

### サマリー
本章の目的は、スケールに伴い増大するインフラコストへ対応策として、量子化技術によるリソース最適化を詳解することにある。AWQ や GPTQ 等の手法選択が、精度維持と計算効率のバランス、さらにはハードウェア寿命に与えるインパクトを評価する。単なるメモリ圧縮に留まらず、KVキャッシュの効率化を含めたスループット向上と、将来的な TCO（総保有コスト）削減を両立させるための導入判断基準を提示する。

---

### 1. 量子化によるリソース制約の再定義
量子化の最大の恩恵は、モデルの重みを 16bit から 4bit や 8bit へ圧縮することで、高価な GPU の必要枚数を物理的に削減できる点にある。

* **メモリフットプリントの劇的圧縮**
  70B クラスのモデルを FP16 で運用する場合、2枚以上の H100（160GB）が必須となるが、4bit 量子化（AWQ等）を施すことで約 40GB 程度に収まり、シングル GPU での運用が可能となる。
* **計算効率とメモリ帯域のトレードオフ**
  量子化はメモリ帯域制限（Memory-bound）下にある推論処理において、データ転送量を減らすことでスループットを向上させる。計算時のデコード処理のオーバーヘッドはあるものの、バッチサイズを拡大できるため、高トラフィック環境下では実効スループットが劇的に改善する。

### 2. KVキャッシュの圧縮と持続的な運用
モデルの重みだけでなく、推論中に生成される KVキャッシュの増大をいかに制御するかが、長期運用の安定性を左右する。

* **KVキャッシュ量子化（INT8/FP8）の導入**
  入力トークンが長くなるにつれ、GPUメモリの大半を KVキャッシュが占有するようになる。重みと同様に KVキャッシュも INT8 等で量子化することで、精度を維持したままメモリ消費量を半減させ、より長いコンテキストや大規模な同時リクエストへの対応を可能にする。
* **持続的なスケーラビリティの確保**
  キャッシュ領域を圧縮することで、GPUメモリのスワップ（CPU側への退避）の頻度を下げ、推論レイテンシのスパイクを抑制する。これは、ユーザー数やデータ量が増大し続ける実運用フェーズにおいて、インフラの追加投資を最小限に抑えつつ安定したサービス品質を維持するための必須技術である。

### 3. 量子化手法の選択と戦略的インパクト
プロジェクトの特性により、採用すべき手法は異なる。

* **AWQ (Activation-aware Weight Quantization)**
  重要な重みを保護しながら量子化する。特にチャット等の汎用タスクにおいて精度劣化が極めて少なく、現在の商用環境で最も使われる手法である。
* **GPTQ (Post-Training Quantization)**
  広く普及している手法。エコシステムが充実しており汎用性が高いが、極端な低ビット化では精度維持が難しくなる傾向がある。

---

#### Q5：顧客の予算が限られている一方で、LLM 推論において高いスループットを要求された場合、どのようなコスト最適化戦略を提案すべきか？

**回答の論理構成例：**
単なる安価なGPUへの変更ではなく、量子化技術を軸に、ハードウェアの利用効率を極限まで高める戦略を提示する。

1. **モデルと KVキャッシュの量子化によるリソース集約**
   モデルの重み（AWQ 等）と KVキャッシュの両方に量子化を適用することを提案する。これによりGPUメモリの消費を抑え、複数枚必要だった GPU を集約し、初期投資を削減する。
2. **スループット最大化によるユニットコストの低減**
   GPUメモリの節約分をバッチサイズの拡大に充て、1回あたりの演算効率を最大化する。結果として、1GPU あたりの同時処理リクエスト数を向上させ、1トークンあたりの単価を最小化する。
3. **持続可能な運用を見据えた拡張性**
   KVキャッシュ圧縮により、将来的な長文入力やトラフィック増大に対しても、現在のインフラ構成のまま対応可能であることを強調する。これは突発的なコスト増を防ぎ、長期的な TCO 削減に寄与する。


<br><br><br>


# 第6章：【拡張設計】マルチノード・クラスタと高速インターコネクト

### サマリー
単一ノードの計算資源には物理的限界があり、超大規模モデルの学習や数千ユーザーを支える推論基盤ではマルチノード構成への拡張が不可欠となる。本章では、ノード間通信で登場する各種技術について説明をする。

---

### 1. 通信の壁とマルチノードの必然性
モデル並列や大規模なデータ並列を行う際、ノード間での重みや勾配の同期処理がシステム全体のボトルネックとなる。

* **標準ネットワーク（TCP/IP）の限界**
  一般的な 10GbE 等の TCP/IP 通信では、OS のプロトコルスタックによる CPU 負荷と遅延が大きく、GPU の演算速度に対して通信が追いつかない。この結果、GPU がデータ待ちで停止する通信待ちによる Starvation が発生する。
* **物理的な広帯域要求**
  H100 等の最新チップをフル活用するには、ノード外への通信にも NVLink に準ずる数百 Gbps 級の帯域（InfiniBand や RoCE）が要求される。



### 2. 高速インターコネクトの核心技術：RDMA と NCCL

* **RDMA (Remote Direct Memory Access)**
  CPU を介さず、あるノードの GPU メモリから別ノードの GPU メモリへ直接データを転送する技術。これにより CPU 負荷をゼロに抑え、マイクロ秒単位の超低レイテンシ通信を実現する。
* **NCCL (NVIDIA Collective Communications Library)**
  NVIDIA GPU 向けに最適化された通信ライブラリ。ノード内の NVLink とノード間の InfiniBand/RoCE をシームレスに統合し、All-Reduce 等の集団通信アルゴリズムをハードウェア特性に合わせて自動最適化する。

---

#### Q6：大規模学習環境、もしくは大容量モデルを使った推論環境構築のためにマルチノード GPU クラスタを実装する場合、どのような設計プロセスが考えられるか？

**回答の論理構成例：**
単なるサーバーの連結ではなく、計算と通信のオーバーラップを実現するアーキテクチャ設計を提示する。

1. **トポロジー設計とインターコネクトの選定**
   ノード間を InfiniBand もしくは RoCE v2 を採用し、Rail-optimizedネットワーク構成を採用する。
2. **NCCL による集団通信の最適化**
   標準の TCP や MPI は汎用的な通信を目的としており、GPU のトポロジーを考慮していない。NCCL はノード内外の物理的な接続経路を自律的に認識し、最短経路でのデータ転送（Direct P2P 通信等）を自動選択する。これにより、マルチノード環境においても GPU のスケーリング効率を高めることが可能となる。
3. **GPUDirect　RDMA / Storage の採用**
   CPUメモリを介さずにGPU間でデータを送受信するため、標準的なTCP通信のようなコンテキストスイッチやコピーのオーバーヘッドが発生せず、圧倒的な広帯域を実現する。
4. **ネットワークトポロジの最適化**
   マルチノード環境においては、スイッチでのパケットロスや輻輳がわずかでも発生すればパケット再送による遅延スパイクは避けられず、システム全体の実行効率を著しく損なう。そのため、ロスレス・イーサネットの設定やスパイン・リーフ構成によるノンブロッキングなファブリック設計を最優先事項として完遂し、物理レイヤーにおける通信停滞を徹底して排除する。

<br><br><br>


# 第7章：【高度な運用】Cloud Nativeな実装

### サマリー
変化の速い AI サービスにおいて、インフラは一度構築して終わりではない。需要に応じてリソースをオートスケールさせる必要が次なる課題となる。本章では、Kubertnetes（GKE）を軸とした実用的なインフラ設計を説明する。なお、GKE 環境における LLM 推論基盤のオートスケーリングにおいて、最大の課題はGPUリソースの確実な確保と巨大なモデルデータのロードによるレイテンシ（コールドスタート）の両立である。

---

### 1. コールドスタートの最小化
LLM の重みデータ（数GB〜数十GB）をストレージから GPU メモリに転送する時間を短縮する必要がある。
* **Image Streaming (Artifact Registry)**: GKE のイメージストリーミング機能を有効化する。ダウンロード率が数%の段階でコンテナをRunning状態に移行させることができ、残りのデータは、アプリケーションが動作している裏側で並行してダウンロードされる。コンテナイメージの全データを待たずに実行を開始できるため、起動時間を大幅に短縮できる。

### 2. 推論ワークロードに最適化されたスケーリング指標
LLM 推論基盤におけるオートスケーリングのトリガーとして、リクエストの収容限界に直結する KV キャッシュの占有率を採用することで、変動の幅が大きいGPUの演算負荷に左右されず、物理的なメモリの空き状況に基づいた正確な Pod の増減が可能となる。
* **Custom Metrics によるスケーリング**: Cloud Monitoring 経由で、推論エンジン（vLLM）が書き出すメトリクスを使用する。
    * **Queue Length (リクエスト待ち行列)**: キューに溜まっている数に基づき、事前に Pod を増やす。
    * **KV Cache Utilization**: GPU メモリ内の KV キャッシュ占有率をトリガーにする。

### 3. プロアクティブなリソース確保とコスト管理
GPUノードはプロビジョニングに数分かかるため、トラフィックが来てから動くのでは遅すぎる。
* **PriorityClass と Pause Pods (Over-provisioning)**: 低優先度のPause Pod（ダミーコンテナ）を事前にデプロイして GPU 枠を確保しておく。本番リクエストが増えた際、HPA が推論 Pod を立ち上げると、Kubernetes のスケーラーが Pause Pod を追い出し（Preemption）、即座に推論 Pod がデプロイされる。
* **Spot GPU の活用**: コストを最大 60-91% 削減できる。ただし、中断耐性が必要なため、推論リクエストの再試行ロジックと、オンデマンド GPU との混合ノードプール構成を必須とする。


---

#### Q7：GKE ベースの LLM 推論インフラにおいて、GPU のコールドスタートを最小化しつつ、長期的なコスト効率を最適化するオートスケーリング戦略をどのように設計するか？

**回答の論理構成例：**
LLM 推論基盤におけるオートスケーリング戦略は、データの高速ロード、GPUを使った推論に特化した指標による KEDA 制御、プロアクティブなリソース確保とコスト最適化の 3 点を統合して設計する。

**1. コールドスタートの最小化**
数 GB 〜 数十 GB に及ぶ LLM の重みデータを、ストレージから GPU メモリへ転送する時間を極小化する。

* **Image Streaming (Artifact Registry)**: GKE のイメージストリーミング機能を有効化する。コンテナイメージのダウンロード率が数%の段階で Running 状態へ移行させ、アプリケーションの裏側で残りのデータを並行ダウンロードすることで、起動時間を大幅に短縮する。

**2. 推論ワークロードに最適化された KEDA によるスケーリング指標**
標準的な CPU やメモリ使用率ではなく、リクエスト収容限界に直結する指標をトリガーに採用する。Cloud Monitoring を経由し、推論エンジン vLLM が公開するメトリクスを **KEDA (Kubernetes Event-driven Autoscaling)** で監視・制御する。

* **KV Cache Utilization**: GPU メモリ内の KV キャッシュ占有率をトリガーとする。GPU 演算負荷の変動に左右されず、物理的なメモリ空き状況に基づいた正確な Pod の増減が可能となる。
* **Queue Length (リクエスト待ち行列)**: vLLM 内部のキューに溜まっているリクエスト数に基づき、リソースが枯渇する前に先行して Pod を増やす。

**3. プロアクティブなリソース確保とコスト管理**
GPU ノードのプロビジョニング遅延（数分単位）を回避しつつ、TCO を削減する。

* **PriorityClass と Pause Pods (Over-provisioning)**: 低優先度の Pause Pod（ダミーコンテナ）を事前にデプロイして GPU 枠を確保しておく。トラフィック増に伴い KEDA が推論 Pod の起動を指示した際、Kubernetes のスケーラーが Pause Pod を即座に追い出し（Preemption）、確保済みの枠に推論 Pod をデプロイすることで、待機時間ゼロのスケーリングを実現する。
* **Spot GPU の戦略的活用**: 計算リソースにスポットインスタンスを採用し、コストを最大 60-91% 削減する。中断耐性を高めるため、オンデマンド GPU との混合ノードプール構成、およびアプリケーション側での再試行ロジックの実装を必須とする。



